### Swap

This role configures a swap file on a Linux system. It installs the `util-linux` package, creates a 
swap file of specified size, sets appropriate permissions, formats the file as swap, enables the 
swap file, adds it to `/etc/fstab`, and checks the swap status.