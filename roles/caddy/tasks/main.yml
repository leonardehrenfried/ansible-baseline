---
- name: Install caddy
  ansible.builtin.package:
    name:
      - caddy

- name: Start systemd service
  systemd:
    name: "{{ item }}"
    state: started
    enabled: yes
    daemon_reload: yes
  with_items:
    - caddy.service
