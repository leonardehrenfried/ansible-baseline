# Prometheus configuration file
global:
  scrape_interval: {{ prometheus_scrape_interval }}
  evaluation_interval: {{ prometheus_scrape_interval }}

scrape_configs:
{{ prometheus_scrape_jobs | to_nice_yaml(indent=2) | indent(2, first=True) }}
